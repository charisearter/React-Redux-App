import React, { Fragment } from 'react'

const FoxForm = props => {
  return (
    <Fragment>
      <h2>Add some floofy adorable cuteness in your life! </h2>
      <button> Floof!</button>
    </Fragment>
  )
};

export default FoxForm;
