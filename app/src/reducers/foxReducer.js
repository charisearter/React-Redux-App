const initialState = {
  foxes: {}
};


// export const getFoxes = () => {
//   return {


//   }
// };

export const foxReducer = (state= initialState, action) => {
  switch(action.type){
    //case FETCH_FOXES
    default:
      return state;
  }
};